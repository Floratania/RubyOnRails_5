<h1><%= action_name == 'new' ? "Нова витрата" : "Редагувати витрату" %></h1>
<%= render 'form', expense: @expense %>
<%= link_to "Назад", expenses_path, class: "btn btn-secondary" %>

<div class="mt-4">
  <%= link_to 'Додати нову категорію', new_category_path, class: 'btn btn-outline-primary me-2' %>
  <%= link_to 'Додати новий метод оплати', new_payment_method_path, class:  'btn btn-outline-primary me-2' %>
</div>